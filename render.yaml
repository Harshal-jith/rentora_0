services:
  - type: web
    name: rentora-kerala
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn rentora.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4
